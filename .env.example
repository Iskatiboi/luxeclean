# Copy this file to .env and fill in real values before deploying.

# Endpoint the Contact and Request-a-Quote forms POST to (e.g. a Formspree
# endpoint, Netlify Forms proxy, or your own serverless function). Until this
# is set, both forms validate correctly but clearly report that no email
# service is connected instead of silently failing.
PUBLIC_FORM_ENDPOINT=
