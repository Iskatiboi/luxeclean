---
import PlaceholderImage from "./PlaceholderImage.astro";
import PillButton from "./PillButton.astro";

interface Props {
  title: string;
  description: string;
  ctaLabel: string;
  ctaHref: string;
  imageLabel: string;
}

const { title, description, ctaLabel, ctaHref, imageLabel } = Astro.props;
---

<div class="flex flex-col border border-ink/10">
  <div class="aspect-[16/10]">
    <PlaceholderImage label={imageLabel} />
  </div>
  <div class="flex flex-1 flex-col gap-3 p-8">
    <h3 class="font-heading text-2xl">{title}</h3>
    <p class="flex-1 font-body text-ink/75">{description}</p>
    <PillButton href={ctaHref} class="mt-2 self-start">{ctaLabel}</PillButton>
  </div>
</div>
